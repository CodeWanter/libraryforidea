<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wf.industry.mapper.ResInfoMapper">
	<!-- 通用查询结果列 -->
	<sql id="Base_Column_List">
		resId, resName, resTblName AS resTblName, resDesc AS resDesc
	</sql>
	
	<select id="getAllTableName" resultType="Map">
		SELECT
		t.resId, t.resName AS resName,t.resTblName AS resTblName,t.resDesc AS resDesc
		FROM tb_res t where true
	</select>
</mapper>